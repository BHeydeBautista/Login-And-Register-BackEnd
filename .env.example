# NestJS
PORT=3000

# JWT (requerido en producción)
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d

# CORS (opcional; separa por comas)
# CORS_ORIGIN=http://localhost:3001

# DB (alternativa a DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=app

# O usa DATABASE_URL:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app
